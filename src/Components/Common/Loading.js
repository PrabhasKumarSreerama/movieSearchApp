const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen bg-gray-100">
      <h1 className="text-xl font-semibold text-blue-600 animate-pulse">
        Data is Loading...
      </h1>
    </div>
  );
};

export default Loading;
